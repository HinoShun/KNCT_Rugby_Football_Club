<h1>詳細画面</h1>
  <%= image_tag(@article.image) %>
  <%= @article.title %>
  <%= @article.content %>

  <% if user_signed_in? && current_user.id == @article.user_id %>
    <div class="">
      <%= link_to "編集する", "#" %>
      <%= link_to "削除する", "#" %>
    </div>
  <% end %>